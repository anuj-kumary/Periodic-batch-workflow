name: DBMS_FEATURE_IM_ADO
fullyQualifiedName: oracle_test.default.sys.DBMS_FEATURE_IM_ADO
storedProcedureCode:
  language: SQL
  code: |-
    AS
    procedure DBMS_FEATURE_IM_ADO
        (feature_boolean  OUT  NUMBER,
         aux_count        OUT  NUMBER,
         feature_info     OUT  CLOB)
        feature_usage         varchar2(1000);
        num_im_ado_pol        number := 0;
        num_obj_pol           number := 0;
        im_size               number := 0;

    begin
        -- initialize
        feature_boolean := 0;
        aux_count       := 0;

        -- check for ILM usage by counting policies in ILM dictionary
        select count(*) into num_im_ado_pol
          from sys.ilmpolicy$
         where pol_subtype = 1;

        if (num_im_ado_pol > 0) then

         -- check the value of the parameter "inmemory_size" from
         -- all instances
         execute immediate
            'select nvl(max(value),0) from gv$parameter where ' ||
            'name = ''inmemory_size'''
          into im_size;

         if (im_size > 0) then
            feature_boolean := 1;

            select count(*) into num_obj_pol
              from sys.ilmobj$
             where policy# in (select policy# from sys.ilmpolicy$
                              where pol_subtype = 1);

            feature_usage   :=
                    'Number of IM ADO Policies: ' || to_char(num_im_ado_pol) ||
            ', ' || 'Number of Objects Affected: ' || to_char(num_obj_pol);
            feature_info    := to_clob(feature_usage);

         end if;
       end if;
    end;
dataProducts: []
storedProcedureType: StoredProcedure
databaseSchema:
  id: a11e2293-c7ca-4d3a-9e0f-ebcfa337159d
  type: databaseSchema
  name: sys
  fullyQualifiedName: oracle_test.default.sys
  displayName: sys
  deleted: false
database:
  id: 635299d9-9c3d-4ccc-98ff-dfb95889d8d6
  type: database
  name: default
  fullyQualifiedName: oracle_test.default
  displayName: default
  deleted: false
service:
  id: 81062063-d522-406b-92a3-ccad1f68f1ad
  type: databaseService
  name: oracle_test
  fullyQualifiedName: oracle_test
  displayName: oracle_test
  deleted: false
serviceType: Oracle
owners: []
tags: []
domains: []
sourceHash: 15f5462b7022c27683ce4f89258fbe34
processedLineage: false
entityStatus: Approved
