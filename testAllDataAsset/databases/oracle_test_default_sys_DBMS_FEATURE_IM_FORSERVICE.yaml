name: DBMS_FEATURE_IM_FORSERVICE
fullyQualifiedName: oracle_test.default.sys.DBMS_FEATURE_IM_FORSERVICE
storedProcedureCode:
  language: SQL
  code: |-
    procedure DBMS_FEATURE_IM_FORSERVICE(
        feature_boolean OUT NUMBER,
        aux_count       OUT NUMBER,
        feature_info    OUT CLOB)
    AS
        feature_usage               varchar2(1000);
        num_tablespaces             number;
        num_tab                     number;
        num_tab_part                number;
        num_tab_subpart             number;
    BEGIN
        feature_boolean             := 0;
        aux_count                   := 0;
        num_tablespaces             := 0;
        num_tab                     := 0;
        num_tab_part                := 0;
        num_tab_subpart             := 0;

        execute immediate
            'select count(*) from dba_tables where ' ||
            'inmemory_service = ''USER_DEFINED'''
        into num_tab;

        execute immediate
            'select count(*) from dba_tab_partitions where ' ||
            'inmemory_service = ''USER_DEFINED'''
        into num_tab_part;

        execute immediate
            'select count(*) from dba_tab_subpartitions where ' ||
            'inmemory_service = ''USER_DEFINED'''
        into num_tab_subpart;

        execute immediate
            'select count(*) from dba_tablespaces where ' ||
            'def_inmemory_service = ''USER_DEFINED'''
        into num_tablespaces;

        --Summary
        feature_usage :=
            ' In-Memory For Service Feature Usage: ' ||
                    'In-Memory USER_DEFINED For Service Tables: ' ||
                      to_char(num_tab) ||
            ', ' || 'In-Memory USER_DEFINED For Service Table Partitions: ' ||
                      to_char(num_tab_part) ||
            ', ' || 'In-Memory USER_DEFINED For Service Table Subpartitions: ' ||
                      to_char(num_tab_subpart) ||
            ', ' || 'In-Memory USER_DEFINED For Service Tablespaces: ' ||
                      to_char(num_tablespaces);

         if ((num_tab > 0) OR (num_tab_part > 0) OR (num_tab_subpart > 0) OR
             (num_tablespaces > 0)) then
          feature_boolean := 1;
          feature_info := to_clob(feature_usage);
         end if;
    END;
dataProducts: []
databaseSchema:
  id: a11e2293-c7ca-4d3a-9e0f-ebcfa337159d
  type: databaseSchema
  name: sys
  fullyQualifiedName: oracle_test.default.sys
  displayName: sys
  deleted: false
database:
  id: 635299d9-9c3d-4ccc-98ff-dfb95889d8d6
  type: database
  name: default
  fullyQualifiedName: oracle_test.default
  displayName: default
  deleted: false
service:
  id: 81062063-d522-406b-92a3-ccad1f68f1ad
  type: databaseService
  name: oracle_test
  fullyQualifiedName: oracle_test
  displayName: oracle_test
  deleted: false
serviceType: Oracle
owners: []
tags: []
domains: []
sourceHash: 5f05a93a6162ce6f22f6771af9876688
processedLineage: false
entityStatus: Approved
