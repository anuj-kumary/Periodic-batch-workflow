name: 80db8eaa959a83fa1be38c88e40c2397
fullyQualifiedName: hktest.80db8eaa959a83fa1be38c88e40c2397
incrementalChangeDescription:
  fieldsAdded: []
  fieldsUpdated: []
  fieldsDeleted:
    - name: queryUsedIn
      oldValue: "[{\"id\":\"f541ed9b-43c8-4fde-9898-ccab88cd5324\",\"type\":\"table\"\
        ,\"name\":\"ORDERS\",\"fullyQualifiedName\":\"hktest.SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.ORDERS\"\
        ,\"description\":\"Orders data as defined by TPC-H\",\"displayName\":\"ORDERS\"\
        ,\"deleted\":false}]"
  previousVersion: 0.4
  changeSummary: {}
owners: []
duration: 303.0
users: []
query: "SELECT count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"5.000 to 5.455\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"5.455 to 5.909\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"5.909 to\
  \ 6.364\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"6.364 to 6.818\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"6.818 to 7.273\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"7.273 to\
  \ 7.727\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"7.727 to 8.182\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"8.182 to 8.636\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"8.636 to\
  \ 9.091\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"9.091 to 9.545\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"9.545 to 10.000\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"10.000 to\
  \ 10.455\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"10.455 to 10.909\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"10.909 to 11.364\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"11.364 to\
  \ 11.818\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"11.818 to 12.273\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"12.273 to 12.727\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"12.727 to\
  \ 13.182\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"13.182 to 13.636\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"13.636 to 14.091\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"14.091 to\
  \ 14.545\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"14.545 and up\" \nFROM \"TPCH_SF1\".\"ORDERS\"\n LIMIT ?"
checksum: 80db8eaa959a83fa1be38c88e40c2397
queryDate: 1752192000000
usedBy:
  - TESTOPENMETADATA
tags: []
queryUsedIn:
  - id: f541ed9b-43c8-4fde-9898-ccab88cd5324
    type: table
    name: ORDERS
    fullyQualifiedName: hktest.SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.ORDERS
    description: Orders data as defined by TPC-H
    displayName: ORDERS
    deleted: false
processedLineage: false
service:
  id: 28293590-2cd0-4d6a-ad7c-bf50802ee2dd
  type: databaseService
  name: hktest
  fullyQualifiedName: hktest
