name: e1aa9460db8dafc2bb113e86036e101a
fullyQualifiedName: hktest.e1aa9460db8dafc2bb113e86036e101a
incrementalChangeDescription:
  fieldsAdded: []
  fieldsUpdated: []
  fieldsDeleted:
    - name: queryUsedIn
      oldValue: "[{\"id\":\"b8edd128-e9a7-4b1e-9a7a-6f441d6c3cb4\",\"type\":\"table\"\
        ,\"name\":\"ORDERS\",\"fullyQualifiedName\":\"hktest.SNOWFLAKE_SAMPLE_DATA.TPCH_SF1000.ORDERS\"\
        ,\"description\":\"Orders data as defined by TPC-H\",\"displayName\":\"ORDERS\"\
        ,\"deleted\":false}]"
  previousVersion: 0.2
  changeSummary: {}
owners: []
duration: 144.0
users: []
query: "SELECT count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"5.000 to 5.312\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"5.312 to 5.625\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"5.625 to\
  \ 5.938\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"5.938 to 6.250\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"6.250 to 6.562\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"6.562 to\
  \ 6.875\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"6.875 to 7.188\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"7.188 to 7.500\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"7.500 to\
  \ 7.812\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"7.812 to 8.125\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"8.125 to 8.438\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"8.438 to\
  \ 8.750\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"8.750 to 9.062\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"9.062 to 9.375\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"9.375 to\
  \ 9.688\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"9.688 to 10.000\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"10.000 to 10.312\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"10.312 to\
  \ 10.625\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"10.625 to 10.938\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"10.938 to 11.250\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"11.250 to\
  \ 11.562\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"11.562 to 11.875\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"11.875 to 12.188\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"12.188 to\
  \ 12.500\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"12.500 to 12.812\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"12.812 to 13.125\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"13.125 to\
  \ 13.438\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"13.438 to 13.750\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\"\
  ) END) AS \"13.750 to 14.062\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND\
  \ LEN(\"O_ORDERPRIORITY\") < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"14.062 to\
  \ 14.375\", count(CASE WHEN (LEN(\"O_ORDERPRIORITY\") >= ? AND LEN(\"O_ORDERPRIORITY\"\
  ) < ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"14.375 to 14.688\", count(CASE WHEN\
  \ (LEN(\"O_ORDERPRIORITY\") >= ?) THEN LEN(\"O_ORDERPRIORITY\") END) AS \"14.688\
  \ and up\" \nFROM \"TPCH_SF1000\".\"ORDERS\"\n LIMIT ?"
checksum: e1aa9460db8dafc2bb113e86036e101a
queryDate: 1752105600000
usedBy:
  - TESTOPENMETADATA
tags: []
queryUsedIn:
  - id: b8edd128-e9a7-4b1e-9a7a-6f441d6c3cb4
    type: table
    name: ORDERS
    fullyQualifiedName: hktest.SNOWFLAKE_SAMPLE_DATA.TPCH_SF1000.ORDERS
    description: Orders data as defined by TPC-H
    displayName: ORDERS
    deleted: false
processedLineage: false
service:
  id: 28293590-2cd0-4d6a-ad7c-bf50802ee2dd
  type: databaseService
  name: hktest
  fullyQualifiedName: hktest
